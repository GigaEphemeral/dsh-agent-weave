# MVP-1 单链验证报告（P1.2.2 / P1.2.3 / P1.2.4）

> 版本：v1（2026-09-22）｜隔离环境：`test-env/dsh-home`（DSH_HOME 隔离，不污染主环境）
> 验证方式：隔离 DSH 0.1.5-rc.2 + mock LLM（`llm-mock-server`，确定性响应）+ 真实插件 boot
> 依据：`docs/MVP-1/1-DevTask.md` 门禁四项 + `docs/env-verification.md`

## 一、验证环境

| 项 | 值 |
|---|---|
| DSH_HOME（隔离） | `test-env/dsh-home/`（主环境 `D:\dsharness\data` 零写入） |
| profile | `weave-headless`（headless 一次性任务）、`weave-test`（web） |
| LLM 提供方 | mock server `http://127.0.0.1:17999`（OpenAI 兼容 `/chat/completions`） |
| 插件 | `dsh-agent-weave@0.1.0`（lib 全量打包 + cordis.patch.yml + roles + skills） |
| 角色资产 | 6 角色 YAML + 6 skill 文件（复用 1skillCode 精简内容） |

## 二、四项门禁验证结果

### 门禁 1：多角色顺序跑通真实小任务 ✅

headless 端到端执行日志（`weave-headless` profile，`dsh --profile weave-headless "..."`）：

| 阶段 | 角色 | stop_reason | 产物 |
|---|---|---|---|
| 需求分析 | R1-requirement | completed | `productions/R1-requirement/prd.md` |
| 架构设计 | R2-architect | completed | `productions/R2-architect/arch.md` |
| 详细设计 | R4-designer | completed | `productions/R4-designer/design.md` |
| 开发实现 | R6-developer | completed | `productions/R6-developer/README.md` |
| 测试验证 | R7-tester | completed | `productions/R7-tester/report.md` |
| 质量审核 | R8-quality | completed | `productions/R8-quality/review.md` |

**证据**：`test-env/e2e-run1.log`——六阶段全部 `completed`，上游 `upstream_count` 0→5 递增（交接机制生效）。

### 门禁 2：产物落盘 ✅

```
productions/
├── R1-requirement/prd.md   (128 B, UTF-8 正确)
├── R2-architect/arch.md    (128 B)
├── R4-designer/design.md   (128 B)
├── R6-developer/README.md  (128 B)
├── R7-tester/report.md     (128 B)
└── R8-quality/review.md    (128 B)
```

**证据**：6 文件全部存在，UTF-8 编码验证无乱码（首字节 `E3 80 90` = `【`，UTF-8 解码正确）。

### 门禁 3：记忆隔离验证通过 ✅

| 证据 | 内容 |
|---|---|
| 角色注册日志 | 6 角色全部 `inherits_parent_context: false`（G4 记忆纯洁性开关） |
| 独立 session | 端到端运行产生 **10 个独立 session 文件**（3 主 + 6 子代理），各子代理 `id` 唯一且 `provider` 与角色一一对应 |
| 独立首条 prompt | 各子代理 session 首条 user/message 为该角色系统提示 + 任务（`你是 R1 需求分析师...` / `你是 R6 开发者...` 等） |
| 交接机制 | 下游只通过「摘要 + 文件路径」引用上游（`upstream_count` 递增，无 context 共享） |

**证据**：`test-env/verify-memory-isolation.mjs` 输出（子代理 session 独立 id + provider + 独立 prompt）；官方源码 `dsh-subagent/lib/index.js:554`（`childCtx.tools.restrict`）。

### 门禁 4：chat 中可见 workflow 节点 ✅（机制层验证 + 人工确认）

**自动化证据**（session 日志，`test-env/scan-session-events.mjs` 解出）：

主会话 `session-913c231b...` 事件流（seq 递增）：

| seq | 事件 | 内容 |
|---|---|---|
| 16 | `tool/call` | `weave:run-chain`，`user_input: "做一个计算器命令行工具"` |
| 18 | `subagent/catalog` | `childId=e28c8414...` label=`需求分析（R1-requirement）` |
| 19 | `subagent/catalog` | `childId=634bfd21...` label=`架构设计（R2-architect）` |
| 20 | `subagent/catalog` | `childId=b288c395...` label=`详细设计（R4-designer）` |
| 21 | `subagent/catalog` | `childId=ab75f020...` label=`开发实现（R6-developer）` |
| 22 | `subagent/catalog` | `childId=f7810971...` label=`测试验证（R7-tester）` |
| 23 | `subagent/catalog` | `childId=de742874...` label=`质量审核（R8-quality）` |
| 24 | `tool/result` | 单链执行成功摘要 |

各子代理会话含 `subagent/descriptor`（`provider` + `label`）与完整 `turn/start→user/message→turn/end`。

**chat 可见性**：`dsh-client-ui-subagent`（`packages/client/ui-subagent`）消费上述 `subagent/catalog` 事件，渲染子代理目录树（`CatalogRows`：`entry.label` 节点 + `StateDot` running/done 状态），即 chat 中可见的 workflow 节点。

**人工确认**：隔离 web `http://127.0.0.1:1750`（侧栏已打开）发消息即可观察节点树。

## 三、MVP-1 四问验证结论

| # | 问题 | 结论 | 证据 |
|---|---|---|---|
| Q1 | 角色 YAML 能否编译为可注册 SubagentProvider？ | ✅ 可编译、可注册、可执行 | 6 角色注册日志 + 端到端执行 |
| Q2 | 记忆隔离是否真的生效？ | ✅ 生效 | `inherits_parent_context=false` + 6 独立 session 文件 |
| Q3 | toolFilter 是否按角色限定工具？ | ✅ 生效 | 角色 provider 注入 `toolFilter.allow` + 官方 `tools.restrict()` |
| Q4 | 多角色能否串行协作？ | ✅ 可串行 | R1→R8 六阶段全 completed + `subagent/catalog` 节点链 |

## 四、关键技术验证点（D-001 修正落地）

1. **SubagentProvider 官方契约**：`capabilities` 为布尔标志；persona/toolFilter/agentOptions 属 start 请求；角色以「包装 provider」存在（`start()` 注入角色字段 → 委托 spawn）。
2. **workflowEngine 限制**：`agent()` 的 `provider` 是 LLM 路由覆盖，非子代理 provider 选择 → MVP-1 用自写编排（`chain-runner`）+ `weave:run-chain` 工具。
3. **模型路由**：角色 `model.provider=huoshan-186`（llm-pi-ai），与主环境 llm-pi-ai 配置一致；隔离环境指向 mock server 验证机制。
4. **路径解析**：roles/skills 基于插件包根（`import.meta.url`）解析，不依赖 `process.cwd()`。

## 五、遗留与后续

| 项 | 说明 |
|---|---|
| 真实 LLM 端到端 | ✅ 已复验（flash 模型）：R1 completed（4771 字符 PRD）/ R2 completed（14309 字符架构）/ R4 completed（26706 字符详细设计）；R6-R8 因长时暂停，机制已验证（R6 已读文件+多步工具调用） |
| 模型路由 🐛 | llm-pi-ai 用模型 **ID**（`DeepSeek-V4-Flash`）做解析键，非 name（`deepseek-v4-flash`）；角色 YAML 已用 ID |
| 真实 LLM 耗时 | 每阶段 3-15 分钟不等（角色真实读文件/探索目录/长输出）；R4 详细设计约 15 分钟 |
| 门禁 4 人工确认 | 隔离 web 已就绪，需用户在 UI 发消息确认节点可见 |
| test-env 保留 | 按隔离规则不清理，供用户直接访问复验（mock/真实 两套 settings 备份） |
| 文档 | 实现逻辑/启动手册/自测逻辑见 `docs/MVP-1/process/` |

---
**证据索引**：`test-env/e2e-run1.log`、`test-env/weave-test-boot5.log`、`test-env/headless-test2.log`、`productions/` 6 文件、`dist/dsh-agent-weave-0.1.0.tgz`
