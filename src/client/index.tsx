import type { Context } from '@deepseek-ai/cordis'

/**
 * dsh-agent-weave Client 半端入口。
 *
 * ⚠️ MVP-1 仅为占位空壳，MVP-4 实现画布/看板/审批面板。
 */
export const name = 'dsh-agent-weave-client'

export function apply(_ctx: Context): void {
  // MVP-1 空壳，MVP-4 实现
}
