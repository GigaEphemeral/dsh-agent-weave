# MVP-5 阶段0：四类问题修复记录

> 版本：v1（2026-09-25）｜状态：✅ 引擎/角色层修复完成（UI 层除外）
> 依据：`docs/MVP-5/MVP-5.txt`（问题 1-4）+ `docs/MVP-5/MVP-5-task-union.md`（Phase 0）

## 修复内容

| 问题 | 层 | 实现 |
|---|---|---|
| 1 角色职责越界 | L1+L2 | 角色 YAML 新增 `output` 约束（R1/R2/R4/R7/R8：仅 .md + 禁止代码扩展名）；新增 `output-gate.ts` 节点产物扫描；工具白名单沿用 MVP-4 |
| 2 异常不退出/不传播 | L2+L3+L4 | 新增 `environment-gate.ts`（角色 `environment.preflight`）；失败抛 `EnvironmentGateError` → 分类为 `environment-gate` → 引擎暂停快照 + 通知，禁止静默降级 |
| 3 交付物不可用/无独立验证 | L2 | 图 DSL 新增 `constraints`：`required_phases`（SOP 顺序静态强制）+ `independent_verification`（test/quality 独立于 develop）；`allow_silent_degrade` 字段预留 |
| 4 多角色重复探测 | L3 | 新增 `project-memory.ts`：解析产物 YAML front-matter facts → 跨节点共享 → 下游 prompt 注入“已确认事实，不要重复探测”；冲突后值覆盖并标记 conflict |

## 关键接入点

- 引擎：`state-graph.ts` addSubagent 节点前跑 Environment Gate、prompt 注入 `{{facts}}`、产物落盘后解析 facts + Output Gate
- 运行入口：`cli/graph-run-commands.ts` 创建 `ProjectMemory` 并传入引擎；角色 environment/output 透传
- 角色资产：6 个 `SKILL.md` 追加“输出规范（MVP-5 Facts 契约）”；R1/R2/R4/R7/R8 YAML 追加 `output`

## 验证证据

- `pnpm typecheck`：0 error
- `pnpm build`：通过（host + client）
- `pnpm vitest run`：**51 文件 / 313 测试全绿**（新增 3 个 spec：project-memory 6、environment-gate 5、mvp5-phase0 13，共 29 个）
- 冒烟脚本：facts 提取/注入/冲突、env gate 注入 runner、output gate、constraints 校验、错误分类均通过

## 待办/真人实测建议

1. 真实 LLM 跑一次带 facts front-matter 的六阶段图，确认下游 prompt 出现“已确认事实”且不再重复探测
2. 真实环境验证 Environment Gate 失败 → 图暂停 → `weave_graph_resume` 恢复链路
3. 真实环境验证 R1 越界写 .py 时 Output Gate 拦停（当前 R1 无 pwsh，主要靠 skill 约束）
4. `allow_silent_degrade` 目前是约束字段，运行时“降级检测”仍依赖 Environment Gate + 角色纪律，未做全自动 fallback 识别
