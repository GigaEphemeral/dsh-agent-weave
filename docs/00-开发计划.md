# 可视化 Agent 任务编排工具 · 完整开发计划（含阶段借鉴指南）

> 版本：v4（2026-09-22）｜依据：`04-MVP与设计契约.md`（唯一权威版）
> 定位：**逐阶段详述 + 借鉴资源标注 + 可执行任务清单**
> 命名规范：`P<阶段>.<子阶段>.<序号>`；预研任务用 `PR.<序号>`；借鉴资源标注 📘官方 / 🔧社区 / 📚学术


## 全局架构基线

代码结构从一开始按大目标设计，后续只填充实现，不重构骨架。完整目录结构见 `docs/04` §全局架构基线。核心技术栈：TypeScript strict + NodeNext ESM｜Cordis 4.x｜tsdown ^0.22｜React 18.3｜React Flow｜Zod｜Vitest。


## Phase R：预研阶段（MVP 动工前）

**目的**：在对应 MVP 动工前验证关键依赖的可用性、API 契约、版本兼容性，避免开发中途返工。预研任务可并行执行，结果写入 `docs/03-能力探测`。

### PR.1 `ctx.llm` usage 字段探测

| 项 | 内容 |
|---|---|
| **交付物** | 探测报告 |
| **验收标准** | 确认 `ctx.llm` 是否返回 per-call usage（input/output tokens）；据此定 Token 采集点 |
| **前置** | 无 |
| **预估** | 0.5d |
| **借鉴** | 📘 DSH 官方 `ctx.llm` 接口文档；`@deepseek-ai/dsh-agent-budget` 的 provider-reported usage 替换机制（`dsh-agent-budget` 在每次 LLM 调用前预留 capacity，stream settle 后用 provider-reported usage 替换估算） |

### PR.2 `@deepseek-ai/dsh-agent-budget` 可用性验证

| 项 | 内容 |
|---|---|
| **交付物** | 验证报告 |
| **验收标准** | 确认包名、版本、API、durable session + descendant-tree scopes 是否可用；与当前 DSH 版本兼容 |
| **前置** | 无 |
| **预估** | 1d |
| **借鉴** | 🔧 `vibeinging/dsh-agent-budget`（官方 npm 包 `@deepseek-ai/dsh-agent-budget`）——durable session 和 local descendant-tree scopes，通过 DSH Storage Domain 恢复；soft accounting / hard admission 双模式；绝对 deadline 跨 restart 存活；并发子代理不会花同一份剩余余额。**注意**：该包仓库为私有，需从固定 GitHub commit 安装（`dsh plugin --profile web add -w github:dsh-external/dsh-agent-budget#<reviewed-commit>`） |

### PR.3 跨版本 API 复测（`sendMessage` vs `followup`/`reportFrom`）

| 项 | 内容 |
|---|---|
| **交付物** | 复测报告 |
| **验收标准** | 确认当前 DSH 版本的实际 API 名；`dsh --version` + `npm ls` 输出 |
| **前置** | 无 |
| **预估** | 0.5d |
| **借鉴** | 📘 `packages/subagent/subagent/src/types.ts` 中的 `ContinuableStartSpec` / `SubagentProvider` 类型定义 |

### PR.4 社区插件版本兼容性矩阵

| 项 | 内容 |
|---|---|
| **交付物** | 兼容性矩阵 |
| **验收标准** | 逐个验证 `dsh-discipline-guard` / `dsh-turn-budget` / `dsh-session-pruner` / `dsh-plugin-product-subagents` / `dsh-token-stats` 与当前 DSH 版本兼容 |
| **前置** | 无 |
| **预估** | 1d |
| **借鉴** | 🔧 `dsh-discipline-guard`（haozheou 维护，四道硬闸门，MIT）；🔧 `dsh-turn-budget`（Nunchakus888 维护，MIT，npm 当前 `0.1.0`，默认基线 24 model steps / 40 tool executions per turn）；🔧 `dsh-session-pruner`（mrzhangkris 维护，Apache-2.0，npm 已发布） |

### PR.5 `dsh-session-pruner` 集成验证

| 项 | 内容 |
|---|---|
| **交付物** | 验证报告 |
| **验收标准** | 确认自动归档 one-shot/continuable/main 的行为；`session_projcache.json` 不膨胀 |
| **前置** | PR.4 |
| **预估** | 0.5d |
| **借鉴** | 🔧 `dsh-session-pruner` 的四类生命周期：**one-shot subagent** 完成后秒级归档（事件驱动 + 3min grace）；**continuable subagent** 空闲超 N 天归档（默认关闭）；**main session** 空闲超 N 天归档（默认关闭）；**任意类型**总量超上限（默认 400）按 `one-shot → continuable → main` + 最旧优先回收。归档目录 `~/.dsh/sessions-archive/`，超过 N 小时（默认 24h）物理删除 |

### PR.6 `dsh-turn-budget` / `dsh-discipline-guard` 挂载验证

| 项 | 内容 |
|---|---|
| **交付物** | 验证报告 |
| **验收标准** | 确认四道硬闸门生效；spawn/fork 受 `maxToolCallsPerTurn` 限制 |
| **前置** | PR.4 |
| **预估** | 1d |
| **借鉴** | 🔧 `dsh-turn-budget` 的三个独立上限：`maxStepsPerTurn`（默认 24）、`maxToolCallsPerTurn`（默认 40）、`maxProviderTokensPerTurn`（可选）。所有限制通过公共 Harness 扩展点实现：`agent/pre-step` 拒绝超限模型请求，`tools/pre-execute` 在工具体运行前拒绝超限调用。🔧 `dsh-discipline-guard` 的四道闸门：**Loop breaker**（第 2 次重复注入警告，第 4 次在 dispatch 前 denied）、**Cost fuse**（从 real usage data 计量，fail-closed）、**Route watcher**（模型端点切换时注入一次性警告）、**Plan gate**（每个用户回合的首次写/执行工具调用走原生审批卡） |

### PR.7 Cordis 热重载竞态复现

| 项 | 内容 |
|---|---|
| **交付物** | 复现报告 |
| **验收标准** | 复现 Discussion #2854 的 HMR recompose 竞态；确认 `ctx.effect()` 的正确用法 |
| **前置** | 无 |
| **预估** | 1d |
| **借鉴** | 📘 Cordis 生命周期文档（`ctx.effect` / LIFO 释放）；📘 Discussion #2854（第三方 bundle 插件 boot 后 ~3s 被静默禁用） |

### PR.8 subagent 并发上限实测

| 项 | 内容 |
|---|---|
| **交付物** | 实测报告 |
| **验收标准** | 复现 Discussion #131 的广度爆炸（~56 子代理 → 2.2GB）；确认无内置并发闸 |
| **前置** | 无 |
| **预估** | 1d |
| **借鉴** | 📘 Discussion #131；🔧 `dsh-plugin-product-subagents`（`maxConcurrentChildren` / `idleTimeoutMs`） |

### PR.9 Messages 协议 notice 毒化复现与规避验证

| 项 | 内容 |
|---|---|
| **交付物** | 复现报告 |
| **验收标准** | 复现 Discussion #7065；验证“不依赖 settlement notice 恢复”的可行性 |
| **前置** | 无 |
| **预估** | 1d |

### PR.10 图版本迁移参考调研（AgentGit/CVC）

| 项 | 内容 |
|---|---|
| **交付物** | 调研报告 |
| **验收标准** | 提炼 state commit/revert/branching 语义；映射到 `graphVersion` 字段设计 |
| **前置** | 无 |
| **预估** | 1d |
| **借鉴** | 📚 AgentGit（Git 风格 rollback/branching）；📚 CVC（认知版本控制，Merkle DAG 存储） |

### PR.11-PR.15 生态调研

| 任务 ID | 任务名称 | 交付物 | 借鉴资源 |
|---|---|---|---|
| PR.11 | 观察者生态插件设计逻辑调研 | 调研报告 | 🔧 `dsh-auto-review`（第二模型只读审批，fail-closed 默认）、`dsh-danus`（cold-start 验证者）、`dsh-cortex`（低成本多模型编排与质量门控）、`decision-gates`（五道防偏闸门）、`dsh-ai-team`（客观质量门，`gates_run` 全绿才 approve） |
| PR.12 | 画布生态参考调研 | 调研报告 | 🔧 `dsh-node-flow`（节点类型：Agent/Code/If/Switch/Loop/While；端口连线；Cron 定时；导入导出） |
| PR.13 | 角色导入生态参考调研 | 调研报告 | 🔧 `dsh-agent-studio` / `dsh-expert-team` / `experts-management` |
| PR.14 | `dsh-state-graph` 设计逻辑调研 | 调研报告 | 🔧 `dsh-state-graph`：声明式拓扑（`addNode`/`addEdge`/`addConditionalEdge`）→ 纯函数增量补丁（节点返回 `Partial<State>`，引擎不可变合并）→ 迭代熔断（默认 25 次）→ 轨迹事件流（`graph/*` 事件）→ 节点级 checkpoint 回调 → 审批门节点（`addApprovalGate`）→ 子代理节点（`addSubagent`，经 `ctx.subagents.start(provider, …)` 委托给真实 dsh 子代理）。**核心接口**：`NodeHandler<T>` 签名 `(state, ctx, signal?) => Promise<Partial<T>>`；`ConditionHandler<T>` 返回下一个 `NodeName` / 并行目标数组 / `"__END__"` |
| PR.15 | `dsh-agent-graph` bounded rework 调研 | 调研报告 | 🔧 `dsh-agent-graph`：**有界返工协议**——节点发现上游交付不足时，带证据和验收标准**返回给直接上游**，请求可被 provided / declined / forwarded one hop at a time。**结构化交接**（`summary`/`artifacts`/`openIssues`）注入每个直接下游激活。**分层全局账本**（`README` → `index` → node sections → details），节点共享无对话，每个激活只看到自己的提示、上游交付和账本。**失败行为**：fail-fast（首个节点失败停止运行），`/graph resume` 重试失败和中断的节点 |


## MVP-0：角色资产（已完成）

R1-R10 角色 Skill 已归档，3 横切纪律已定义。**门禁已过。**


## MVP-1：单链脚本验证

**目标**：用 `workflowEngine` 临时验证"角色 = provider + persona"的协作闭环，**不建图**。
**周期**：1 周｜**前置**：MVP-0 完成

### P1.1 角色 Provider 封装

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P1.1.1 | 插件脚手架搭建 | TypeScript, tsdown, Cordis | `package.json`（`dsh.bundle` + `dsh.client`）+ `cordis.patch.yml` + `tsconfig.json` + `tsconfig.client.json` + `tsdown.config.ts` + `src/index.ts` | `dsh plugin --profile web add ./dsh-agent-weave` 能安装；`pnpm build` 产出 `lib/index.js` + `lib/client.js` | 0.5d |
| P1.1.2 | L0-L5 目录骨架 | TypeScript | 按全局架构基线的目录结构 | 所有目录存在；`src/index.ts` 导出空 `apply`；`pnpm build` 通过 | 1d |
| P1.1.3 | 共享类型定义 | TypeScript, Zod | `src/shared/types.ts` + Zod Schema | DevTeamState / Message / GraphDefinitionSpec / RoleDefinition / ObserverSignal 类型完整；`pnpm tsc --noEmit` 无错误 | 1d |
| P1.1.4 | 结构化日志基础设施 | TypeScript, AsyncLocalStorage | `src/shared/logger.ts` | LogEntry 接口完整；`trace_id` 贯穿；脱敏工具（truncate/ref/fingerprint） | 1d |
| P1.1.5 | 角色 YAML Schema | Zod, TypeScript | `src/l3-roles/role-schema.ts` | 含 `schema_version` / `system_prompt_ref` / `model`（完整对象）/ `memory_scope` / `lifecycle` / `max_concurrent_children` / `observers` | 1d |
| P1.1.6 | 角色 Provider 编译器 | TypeScript, `ctx.subagents` | `src/l3-roles/role-loader.ts` 初版 | YAML → SubagentProvider；`model` 传完整 `{ provider, model }` 对象；`toolFilter` 映射；`inheritsParentContext=false` | 2d |
| P1.1.7 | Cordis 生命周期验证 | Cordis `ctx.effect()` | 最小 effect 注册/注销 + 热重载测试 | 插件热重载后无资源泄漏；`ctx.get('skills')` 可选服务探测可用 | 0.5d |

**P1.1 借鉴**：🔧 `dsh-agent-graph` 的节点输出契约——每个节点是 fresh subagent，upstream delivery 作 structured contract 交给 downstream；📘 DSH 官方 `SubagentProvider` 的 `capabilities` 映射（`persona` / `toolFilter` / `agentOptions` / `inheritsParentContext`）。

### P1.2 单链脚本执行

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P1.2.1 | workflowEngine 脚本 | TypeScript, DSH workflowEngine | 串行脚本：R1→R2→R4→R6→R7→R8 | 脚本能跑通；产物落盘到 `productions/<角色>/` | 2d |
| P1.2.2 | 记忆隔离验证 | TypeScript, `ctx.subagents` | 验证报告 | R3 与 R5 的 Session 不共享 context；`inheritsParentContext=false` 生效 | 0.5d |
| P1.2.3 | toolFilter 验证 | TypeScript, `ctx.subagents` | 验证报告 | R6 只能看到自己的工具；`tools`→`toolFilter` 映射生效 | 0.5d |
| P1.2.4 | 单链闭环端到端测试 | Vitest, workflowEngine | 测试脚本 + 运行记录 | 一句话需求输入，自动走完 R1→R8，产出 PRD + 架构 + 代码 + 测试报告 | 1d |

**P1.2 借鉴**：🔧 `dsh-agent-graph` 的 fail-fast 行为（首个节点失败停止运行）和 `/graph resume` 重试机制；🔧 `dsh-swarm` 的工件引用传递（长产出自动落盘共享工件库，消息只传 `art://<id>` 引用 + 200 字摘要）。

**MVP-1 门禁**：多角色顺序跑通真实小任务；产物落盘；记忆隔离验证通过；chat 中可见 workflow 节点。


## MVP-2：自研 StateGraph 引擎（质变点）

**目标**：实现图 DSL、StateGraph 引擎、条件边、循环回退、checkpoint 契约、静态验证器、观察者 L1。
**周期**：3-4 周｜**前置**：MVP-1 + PR.1/PR.2/PR.3/PR.7/PR.8/PR.9/PR.14/PR.15 完成

### P2.1 图 DSL 与契约

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P2.1.1 | 图 DSL Schema 定义 | TypeScript, Zod | `src/l2-engine/graph-definition.ts` | 含 nodes/edges/checkpoint/metadata/observers；`graphVersion` 字段；parallel 边语法预留；Zod 校验 | 2d |
| P2.1.2 | 静态验证器 | TypeScript, Zod | `src/l2-engine/static-validator.ts` | 检查节点/边引用、条件字段存在性、maxIter 已设置、roleRef 已注册；毫秒级返回 | 2d |
| P2.1.3 | checkpoint 契约实现 | TypeScript, Cordis `ctx.effect()`, `ctx.storageDomain`, OTel | `src/l2-engine/checkpoint.ts` | 节点级落盘 `{ graphId, graphVersion, node, state, iteration }`；同一回调双写 RunLedger；所有资源 `ctx.effect()` 注册 | 3d |
| P2.1.4 | Cordis 生命周期约束验证 | Vitest, Cordis HMR | 热重载测试用例 | 插件热重载后无定时器/连接泄漏；**热重载测试入门禁** | 1d |

**P2.1 借鉴**：
- 🔧 `dsh-state-graph` 的 **声明式图定义** `GraphDefinitionSpec`（配置驱动/命令集成），通过 `ctx.graph.fromDefinition(spec)` 从声明式定义构建图；**节点级 checkpoint 回调**（宿主可接 dsh 持久化）；**纯函数增量补丁**（节点只返回 `Partial<State>`，引擎不可变合并）；**迭代熔断默认 25 次**，超限抛错；**双向事件流观测**（`graph/*` 事件，追加式轨迹）；**隔离的状态图实例工厂** `ctx.graph.create<T>(maxIterations?)`。
- 🔧 `dsh-agent-graph` 的**图文档验证**（引擎在运行前校验循环、未知引用、重复 ID）。

### P2.2 StateGraph 引擎核心

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P2.2.1 | StateGraph 引擎骨架 | TypeScript | `src/l2-engine/state-graph.ts` | `addNode` / `addEdge` / `addConditionalEdge` 可用；节点返回 `Partial<State>` | 3d |
| P2.2.2 | 原子合并引擎 | TypeScript | `src/l2-engine/atomic-merge.ts` | 引擎统一原子合并；冲突默认 `reject-on-conflict`；`@mergeable` 字段可 merge；冲突检测在合并阶段 | 2d |
| P2.2.3 | 条件边与循环回退 | TypeScript | `src/l2-engine/condition-edge.ts` | 质量审核输出 `retry` → 回退 + `retry_count++`；`retry_count >= max_iterations` → 审批节点 | 2d |
| P2.2.4 | 全局并发计数 | TypeScript | `src/l2-engine/concurrency-counter.ts` | 节点激活前检查全局活跃 child 计数；超阈值拒绝激活 | 1d |

**P2.2 借鉴**：
- 🔧 `dsh-state-graph` 的**核心接口**：`NodeHandler<T>` 签名 `(state, ctx, signal?) => Promise<Partial<T>>`；`ConditionHandler<T>` 返回下一个 `NodeName` / 并行目标数组 / `"__END__"`；`GraphExecutionResult<T>` 包含 `graphId` / `finalState` / `trajectory` / `iterations`；**并行分支 Fan-out / Fan-in**（条件路由返回目标数组，join 汇聚）；**子代理节点** `StateGraph.addSubagent(name, options)`——经 `ctx.subagents.start(provider, …)` 委托给真实 dsh 子代理，输出映射回父状态。
- 🔧 `dsh-agent-graph` 的 **bounded rework 协议**——节点发现上游交付不足时，带证据和验收标准返回给**直接上游**，请求可被 provided / declined / forwarded one hop at a time。这与"质量审核→开发"回退逻辑高度吻合。
- 📚 **agent-coherence 的 MESI + 乐观并发协议**——将静默覆盖转化为显式的、可重试的类型化冲突；一个过期的写入会被拒绝或作为冲突返回，而不会被静默应用。
- 📚 **LangGraph 的 Reducer 模式**——对可能被并行节点更新的字段用 `Annotated` 标注合并函数（如 `operator.add` 用于列表追加，或自定义字典合并）；合并过程本身是串行的，只需保证结合律。

### P2.3 观察者 L1 与测试

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P2.3.1 | 观察者 L1 轻量检查 | TypeScript | `src/observers/observer-l1.ts` | 纯函数检查（格式、权限、命名规范）；零 Token；返回 `WARN`/`BLOCK` | 2d |
| P2.3.2 | 观察者 L1 与 StateGraph 集成 | TypeScript | 节点预处理钩子 | L1 检查结果影响条件边路由 | 1d |
| P2.3.3 | BDD/mock 测试矩阵 | Vitest | `tests/bdd/` | 路由测试、循环测试、熔断测试、冲突测试全部通过；零 LLM 调用 | 3d |
| P2.3.4 | 挂载 `dsh-turn-budget` 兜底 | `dsh-turn-budget`, TypeScript | 插件配置 + 集成测试 | spawn/fork 作为工具调用时受 `maxToolCallsPerTurn` 限制 | 1d |
| P2.3.5 | 循环工作流端到端测试 | Vitest, workflowEngine | 测试脚本 + 运行记录 | 含循环 DSL 跑通；循环退出 + 熔断双生效 | 2d |

**P2.3 借鉴**：
- 🔧 `dsh-turn-budget` 的 `agent/pre-step` 和 `tools/pre-execute` 扩展点——在派发前拒绝已耗尽额度的工具请求；默认基线 24 model steps / 40 tool executions per turn。
- 🔧 `dsh-discipline-guard` 的 **Loop breaker**——统计每个 agent 的相同工具+参数调用，第 2 次重复注入可见警告，第 4 次在 dispatch 前 denied（2 次连续失败更快触发）。
- 📚 **langgraph-scenario-lab** / **@agentforge/testing** / **pytest-agentkit** 的 mock LLM 和确定性路由测试模式。

**MVP-2 门禁**：含循环 DSL 跑通；循环退出/熔断双生效；checkpoint 契约随 DSL 敲定；路由/条件边确定性测试（零 LLM）；热重载后无资源泄漏；`ctx.llm` usage 与 `dsh-agent-budget` 可用性确认。


## MVP-3：状态 + 交接 + 消息 + 恢复

**目标**：任务树持久化、handoff 四字段、消息总线、checkpoint 恢复、生命周期、并发多层防护、Token 熔断复用、观察者 L2、人工审批分级。
**周期**：3-4 周｜**前置**：MVP-2 + PR.4/PR.5/PR.6/PR.10/PR.11 完成

### P3.1 持久化与交接

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P3.1.1 | 任务树持久化 | TypeScript, `ctx.storageDomain` | `src/l2-engine/task-tree.ts` | 根任务 → 子任务树可写入/读取；复用 `ctx.storageDomain` | 3d |
| P3.1.2 | handoff 四字段交接协议 | TypeScript, `ctx.fs` | `src/l2-engine/handoff.ts` | 产出物落盘 `productions/<角色>/`；下游只读上游产出 | 2d |
| P3.1.3 | RunLedger 审计账本 | TypeScript, `ctx.storageDomain`, OTel | `src/l5-observability/run-ledger.ts` | 不可变事件流；节点级事件完整；只追加；OTel span/trace 对齐 | 3d |
| P3.1.4 | checkpoint 恢复 | TypeScript, `ctx.storageDomain` | 恢复测试脚本 | 中断后从最近 checkpoint（RunLedger）重建；**不依赖 subagent settlement notice** | 2d |

**P3.1 借鉴**：
- 🔧 `dsh-agent-graph` 的**结构化交接版本**（`summary` / `artifacts` / `openIssues`），注入每个直接下游激活；**分层全局账本**（`README` → `index` → node sections → details），节点共享无对话，每个激活只看到自己的提示、上游交付和账本。
- 🔧 `dsh-swarm` 的**任务账本 + 看门狗**：任务状态机 `pending → claimed → done/failed`，失败自动重试（最多 3 次），claimed 超 5 分钟无进展自动重派。
- 🔧 `dsh-swarm` 的**工件引用传递**：长产出自动落盘共享工件库，消息只传 `art://<id>` 引用 + 200 字摘要。

### P3.2 消息总线与生命周期

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P3.2.1 | 消息总线实现 | TypeScript, `ctx.subagents` `sendMessage` | `src/l2-engine/message-bus.ts` | A→父→B 中转；`Message` 结构完整；`correlation_id` + `deadline` | 3d |
| P3.2.2 | 防死锁分级恢复 | TypeScript, Vitest | 死锁测试用例 | 单条超时重试 ≤2；同 Agent 同链路 ≥3 次强制终止；工作流超时降级审批 | 2d |
| P3.2.3 | 生命周期管理 | TypeScript, `dsh-session-pruner` | `src/l3-roles/lifecycle-manager.ts` + 安装 pruner | resident/on-demand/hybrid 生效；冷恢复 SLA >10s 降级人工；session 不膨胀 | 3d |
| P3.2.4 | 并发多层防护 | TypeScript, `dsh-plugin-product-subagents`, `dsh-turn-budget` | 引擎级全局计数 + 两个社区插件挂载 | Provider 层 + Agent Loop 层 + 引擎层三层防护生效 | 2d |

**P3.2 借鉴**：
- 🔧 `dsh-session-pruner` 的**四类生命周期**：one-shot 完成后秒级归档（事件驱动 + 3min grace）；continuable/main 空闲超 N 天归档；总量超上限（默认 400）按 `one-shot → continuable → main` + 最旧优先回收。归档目录 `~/.dsh/sessions-archive/`，超过 24h 物理删除。
- 🔧 `dsh-swarm` 的**独立常驻进程**模式：每个 Agent 是独立 Node.js 进程（`agent-runtime.js`），循环「读 inbox → 跑任务 → 写 outbox」，无需每次冷启动。
- 🔧 `dsh-turn-budget` 的**三个独立上限**（`maxStepsPerTurn` / `maxToolCallsPerTurn` / `maxProviderTokensPerTurn`），通过 `agent/pre-step` 和 `tools/pre-execute` 实现。

### P3.3 Token 熔断与观察者 L2

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P3.3.1 | 复用 `@deepseek-ai/dsh-agent-budget` | `@deepseek-ai/dsh-agent-budget`, TypeScript | 插件配置 + 集成测试 | durable session + descendant-tree scopes 生效；并发子代理不花同一份余额 | 2d |
| P3.3.2 | 挂载 `dsh-discipline-guard` 兜底 | `dsh-discipline-guard`, TypeScript | 插件配置 + 集成测试 | 四道硬闸门生效；模型无法绕过 | 2d |
| P3.3.3 | 观察者 L2 静默观察 | TypeScript, `ctx.fs`, Vitest | `src/observers/observer-l2.ts` | 文件观察（git diff + 读取产出）；80%+ 静默；GREEN/YELLOW/RED 信号；fail-open | 3d |
| P3.3.4 | 人工审批分级 | TypeScript, `ctx.webServer` | `src/l2-engine/approval-gate.ts` | L1 异步/L2 同步/L3 同步+告警；超时策略生效 | 2d |

**P3.3 借鉴**：
- 🔧 `@deepseek-ai/dsh-agent-budget` 的**durable session + descendant-tree scopes**：在每次 LLM 调用前预留 capacity，stream settle 后用 provider-reported usage 替换估算，**并发子代理不会花同一份剩余余额**。输出收敛机制在预算接近耗尽时减少 `maxTokens`。
- 🔧 `dsh-discipline-guard` 的 **Cost fuse**：从 real usage data 计量（input tokens net of cache、cache-hit rate、per-turn cumulative），典型 token-plan 先警告，同 turn 重复违规 **hard-stops the turn**。三个预设模式：`off` / `daily`（默认，capped at ~500K tokens/turn）/ `wild`（只警告不中断）。
- 🔧 `dsh-node-flow` 的**执行反馈**：节点展示运行状态，实际经过的连接路径高亮，方便定位流程走向。

### P3.4 对外配置基础

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P3.4.1 | 角色包加载器 | TypeScript, Zod, YAML | `src/l3-roles/role-package.ts` | YAML 角色包 → `SubagentProvider` + 元数据；版本/依赖校验 | 2d |
| P3.4.2 | 流程包加载器 | TypeScript, Zod, YAML | `src/l3-roles/workflow-package.ts` | YAML 流程包 → `GraphDefinitionSpec`；依赖角色包校验 | 2d |
| P3.4.3 | 重启与目标复用 | `ctx.jobs`, `ctx.goals`, TypeScript | 集成测试 | 后台任务 registry + 目标跟踪生效 | 1d |

**P3.4 借鉴**：🔧 `dsh-agent-graph` 的**图文档**（YAML 图，`needs` 边），引擎在运行前校验循环、未知引用、重复 ID；🔧 `dsh-node-flow` 的**导入与导出**（工作流可保存为文件，便于备份、分享和复用）。

**MVP-3 门禁**：中断可恢复；交接可追溯；按角色分账；跨角色对话可达；session 不膨胀；角色包/流程包可加载。


## MVP-4：只读激活看板

**目标**：节点状态事件流、图渲染、审批面板、观察者信号可视化、画廊视图。
**周期**：2-3 周｜**前置**：MVP-3 完成

### P4.1 事件流与后端

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P4.1.1 | 节点状态事件流 | TypeScript, OTel, `ctx.webServer` | `src/l4-visual/host/event-bus.ts` | 事件类型完整（node-start/end/error/loop-iteration/edge-traversed/checkpoint-written）；OTel 语义对齐 | 3d |
| P4.1.2 | REST/WebSocket 路由 | TypeScript, `ctx.webServer` | `src/l4-visual/host/routes.ts` | 自定义 REST 端点；WebSocket 推送；不走 settings 白名单 | 2d |

**P4.1 借鉴**：🔧 `dsh-state-graph` 的**双向事件流观测**（`graph/*` 事件，追加式轨迹）；🔧 `dsh-graph` 的 **REST 端点** `/api/dsh-graph` 和浏览器二维泳道看板（`conversation.view`）。

### P4.2 前端渲染

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P4.2.1 | 图渲染组件 | React 18, React Flow, CSS Modules | `src/l4-visual/client/dashboard/` | DSL 图结构 + 节点状态染色；React Flow 集成；节点实时更新 | 4d |
| P4.2.2 | 审批面板 | React 18, `dsh-client-ui-primitives` | `src/l4-visual/client/dashboard/approval.tsx` | 审批请求展示；L1/L2/L3 分级；人工介入记录写入 RunLedger | 2d |
| P4.2.3 | 观察者信号可视化 | React 18, React Flow | `src/l4-visual/client/observer/` | GREEN/YELLOW/RED 信号在画布上染色；关注度热力图 | 2d |
| P4.2.4 | 画廊视图 | React 18, Zustand | `src/l4-visual/client/gallery/` | 浏览已安装的角色包和流程模板 | 2d |
| P4.2.5 | Token 消耗面板 | React 18, `dsh-token-stats` | 集成 `dsh-token-stats` | 按角色/节点/模型的 Token 消耗可视化 | 2d |

**P4.2 借鉴**：🔧 `dsh-node-flow` 的**可视化编排**（拖拽节点、连接端口，直观看到数据和执行路径）、**节点属性面板**（选中节点后配置名称、模型、提示词、超时和失败处理策略）、**执行反馈**（节点展示运行状态，实际经过的连接路径高亮）；🔧 `dsh-graph` 的**浏览器二维泳道看板**；🔧 `dsh-swarm` 的**团队面板**（实时显示 Agent 状态与任务账本：⏳pending claimed ✅done dead）。

**MVP-4 门禁**：一次多角色循环全程图节点实时染色；观察者信号可见；Token 消耗可分账。


## MVP-5：角色导入 + 画布连边

**目标**：YAML 导入解析器、画布编辑器、端口规则、source 冲突解决、Gallery 导入/导出、自定义节点插件注册。
**周期**：3-4 周｜**前置**：MVP-4 + PR.12/PR.13 完成

### P5.1 画布编辑器

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P5.1.1 | YAML 导入解析器 | TypeScript, `yaml`, Zod | `src/l3-roles/role-loader.ts` 完整版 | YAML → Provider + L3 元数据；错误提示清晰 | 2d |
| P5.1.2 | 画布编辑器骨架 | React Flow, Zustand, CSS Modules | `src/l4-visual/client/canvas/` | 节点拖入、连线、配置循环；React Flow 集成 | 5d |
| P5.1.3 | 端口规则引擎 | React Flow, TypeScript | `src/l4-visual/client/canvas/ports.ts` | 角色节点输入/输出端口；条件节点 true/false；非法连接阻止 | 3d |
| P5.1.4 | 画布 → DSL 序列化 | TypeScript, Zod | `src/l4-visual/client/canvas/serializer.ts` | 画布状态序列化为合法 GraphDefinitionSpec | 2d |
| P5.1.5 | DSL → 画布反序列化 | TypeScript, React Flow | 同上 | DSL 加载后画布正确渲染 | 2d |

**P5.1 借鉴**：🔧 `dsh-node-flow` 的完整画布设计——**节点类型**（触发器、子 Agent、代码、条件分支、循环、定时任务）、**端口连线**（拖动节点端口建立连接）、**流程控制**（内置 If、Switch、Loop 和 While）、**失败策略**（Code / Agent 节点可设置超时，以及"停止分支"或"继续使用空值"）、**定时任务**（5 段 Cron 表达式，任务面板中查看/立即运行/取消）、**导入与导出**（工作流可保存为文件）。配套 AI Skill `dsh-node-flow-skill` 可生成可直接导入的 JSON 工作流。

### P5.2 生态与插件

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P5.2.1 | source 冲突解决 | TypeScript, Zod | `metadata.source` 标记 + 冲突策略 | yaml/canvas/hybrid 三种来源；冲突时提示用户选择 | 2d |
| P5.2.2 | Gallery 导入/导出 | TypeScript, `ctx.webServer` | `src/l6-ecosystem/gallery-registry.ts` | 文件/URL/市场三种来源；冲突策略 skip/overwrite/rename | 3d |
| P5.2.3 | 自定义节点插件注册 | TypeScript, Cordis | `src/l6-ecosystem/custom-node-registry.ts` | 通过 DSH 插件机制注册新 nodeType；inputSchema/outputSchema/execute | 3d |
| P5.2.4 | 沙箱隔离基础 | Docker / seccomp, TypeScript | 容器级隔离配置 | 用户导入的自定义节点在沙箱内执行 | 3d |
| P5.2.5 | 零代码验证 | Vitest, React Flow | 端到端测试 | 用户零代码搭建自定义团队并跑通 | 2d |

**P5.2 借鉴**：🔧 `dsh-node-flow` 的 **AI Skill 配套**（`dsh-node-flow-skill` 包含工作流文档结构、节点字段、连线端口、循环规则与完整示例，减少字段遗漏或端口名称错误）；🔧 `dsh-state-graph` 的**图注册为 dsh slash 命令**（`ctx.graph.registerCommand()`）。

**MVP-5 门禁**：零代码搭自定义团队跑通；画布产物 = 合法图 DSL；Gallery 可导入/导出；沙箱隔离生效。


## MVP-6：打磨 + 生态

**目标**：恢复加固、超时降级、记忆压缩、对抗评审、观察者 L3、社区分发。
**周期**：3-4 周｜**前置**：MVP-5 完成

| 任务 ID | 任务名称 | 技术栈 | 交付物 | 验收标准 | 预估 |
|---|---|---|---|---|---|
| P6.1 | 恢复加固 | TypeScript, Vitest | 恢复测试矩阵 | 各种中断场景（崩溃/重启/超时）恢复一致 | 3d |
| P6.2 | 超时降级 | TypeScript | 降级策略实现 | 冷恢复 >10s 降级人工；工作流超时降级审批 | 2d |
| P6.3 | 记忆压缩 | TypeScript, `ctx.llm` | `src/l5-observability/memory-compress.ts` | 滑动窗口摘要；LSC 长序列压缩（>80% 减少）；分层记忆（主要目标/副目标/bug 归纳） | 4d |
| P6.4 | 对抗评审 | TypeScript, `ctx.llm`, LLM-judge | LLM-judge 集成 | R8 对立角色；语义评估；连续 2 次不通过触发 L1 审批 | 3d |
| P6.5 | 观察者 L3 深度审查 | TypeScript, `ctx.llm` | `src/observers/observer-l3.ts` | 流式观察者；完整 LLM 审查；BLOCK/SANITIZE/FLAG | 3d |
| P6.6 | 工作区卫生检查 | TypeScript, `ctx.fs` | 卫生检查脚本 | 每角色独立产出目录；定期清理 | 1d |
| P6.7 | 社区分发准备 | npm, DSH Plugin Hub | npm 包 + Plugin Hub 发布 | `dsh plugin --profile web add dsh-agent-weave` 可安装 | 2d |
| P6.8 | 子市场同步 | TypeScript, `ctx.webServer` | `src/l6-ecosystem/marketplace-sync.ts` | 从社区市场同步角色包/流程模板 | 3d |
| P6.9 | 导入安全校验 | TypeScript, Zod | 安全审计流程 | 角色包/流程包的 Schema 校验 + 依赖校验 + 沙箱验证 | 2d |
| P6.10 | 全套门禁回归 | Vitest, CI | 回归测试报告 | 所有 MVP 门禁自动化可复跑 | 3d |

**P6 借鉴**：
- 🔧 `dsh-discipline-guard` 的 **Route watcher**：当会话切换模型端点且历史较长时，注入一次性警告——新端点可能按全价重计整个上下文。
- 🔧 `dsh-agent-graph` 的 **fail-fast 行为**和 `/graph resume` 重试机制。
- 🔧 `dsh-session-pruner` 的**归档优先（可恢复），过期后删除**机制。
- 🔧 `dsh-agent-budget` 的**输出收敛**：当有界账户接近耗尽时减少 `maxTokens`。

**MVP-6 门禁**：全套门禁通过；社区可分发；导入安全校验生效。


## 依赖关系与并行策略

```
Phase R（预研，按 MVP 门禁前置）
    ↓
MVP-0（角色资产，已完成）
    ↓
MVP-1（单链验证）
    ↓
MVP-2（StateGraph 引擎）← 质变点
    ↓
MVP-3（状态+交接+消息+恢复）
    ↓
MVP-4（只读看板）
    ↓
MVP-5（角色导入+画布）
    ↓
MVP-6（打磨+生态）
```

**并行可能性**：
- P3.1（持久化）与 P3.2（消息）可部分并行
- P4.2 图渲染组件与 P5.1 画布编辑器共享 React Flow 基础，可提前准备
- P6.3 记忆压缩与 P6.4 对抗评审可并行
- 预研任务可并行执行（PR.10-PR.15 为文档调研，不阻塞开发）


## 总工时估算

| 阶段 | 任务数 | 预估工时 | 周期 |
|---|---|---|---|
| **预研 Phase R** | 15 | 14.5d | 2-3 周（并行） |
| MVP-0 | 2 | 已完成 | — |
| MVP-1 | 11 | 10d | 1 周 |
| MVP-2 | 14 | 23d | 3-4 周 |
| MVP-3 | 15 | 31d | 4 周 |
| MVP-4 | 7 | 17d | 2-3 周 |
| MVP-5 | 10 | 27d | 3-4 周 |
| MVP-6 | 10 | 26d | 3-4 周 |
| **合计** | **84** | **~148d** | **~18-22 周** |

> 注：预研任务可并行执行，实际关键路径约 ~134d（不含预研）。建议预留 30% buffer。


## 跨阶段关键约束

| 约束 | 适用阶段 | 说明 |
|---|---|---|
| **Cordis 生命周期** | 全阶段 | 非 Cordis 一等服务的资源必须 `ctx.effect()` 注册；禁止裸 `setTimeout`/`fs.open`/`new Database()` |
| **模型显式指定** | MVP-1 起 | `{ provider, model }` 完整对象，不能只传 model 名（🐛 Discussion #4311/#4313） |
| **不依赖 settlement notice 恢复** | MVP-3 起 | 用 RunLedger 重建状态（🐛 Discussion #7065） |
| **热重载测试入门禁** | MVP-2 起 | 每个 MVP 门禁必须包含热重载后无资源泄漏验证 |
| **并发多层防护** | MVP-2 起 | Provider 层 + Agent Loop 层 + 引擎层；进程层为结构性限制 |
| **测试集隔离** | MVP-2 起 | 测试集不参与变异或选版 |
| **默认 observe-only** | MVP-3 起 | 观察者默认只观察和提议，不自动应用 |
| **评分标准隔离** | MVP-3 起 | 回归任务评分标准物理上不进入 agent 上下文 |
| **提案需人工审阅** | MVP-3 起 | 所有变异候选进入待审阅队列 |


## 建议启动顺序

**第 1-3 周（预研）**：并行执行 PR.1-PR.15；重点完成 PR.1（ctx.llm usage）、PR.2（dsh-agent-budget 可用性）、PR.3（跨版本 API 复测）、PR.7（Cordis 热重载竞态）、PR.8（并发上限实测）、PR.9（Messages 协议毒化）
**第 4 周**：MVP-1 启动（插件脚手架 + 角色 Provider 封装）
**第 5 周**：MVP-1 单链闭环验证 → 门禁
**第 6-9 周**：MVP-2 StateGraph 引擎（质变点，集中不被打断）
**第 10-13 周**：MVP-3 状态+交接+消息+恢复
**第 14-16 周**：MVP-4 只读激活看板
**第 17-20 周**：MVP-5 角色导入+画布连边
**第 21-24 周**：MVP-6 打磨+生态
**第 25 周**：缓冲 + 全套门禁回归

**关键里程碑**：
- 第 3 周末：预研完成，关键依赖可用性确认
- 第 5 周末：MVP-1 门禁通过，角色协作闭环可用
- 第 9 周末：MVP-2 门禁通过，**质变点达成**（从"一堆 skill"到"编排工具"）
- 第 13 周末：MVP-3 门禁通过，可持久化/可恢复/可分账
- 第 16 周末：MVP-4 门禁通过，状态可视化可用
- 第 20 周末：MVP-5 门禁通过，零代码自定义可用
- 第 24 周末：MVP-6 门禁通过，社区可分发