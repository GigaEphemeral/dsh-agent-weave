# MVP-4 真实环境回归清单

> 版本：v1（2026-09-24）｜基线：DSH 0.1.5-rc.2 + Windows 11 + Node 22 + ollama-local
> 场景：`test-env/dsh-home/profiles/weave-test`（端口 3081），真实 LLM 端到端。
> 前置：MVP-3 §六 待真实验证 5 项 + MVP-4 新增 Web 看板项。

## 0. 环境准备

- [ ] `pnpm build && pnpm pack --pack-destination ./dist`
- [ ] 新建/复用 `weave-test` profile，`dsh plugin add ./dist/dsh-agent-weave-*.tgz`
- [ ] 启动：`node $DSH_BIN --profile weave-test --port 3081`
- [ ] 检查：`netstat -ano | findstr :3081` 端口占用；Ollama `200`

## 1. 真实 LLM 端到端（MVP-3 §六.1 回归）

- [ ] `weave_run_graph path=<yaml> user_input=<需求> output_dir=<隔离产物目录>`
- [ ] 角色真实产出（角色 YAML → subagent → 产物落 `exec.workspace/productions`）
- [ ] 产物落盘目录正确（`artifactName` / `<nodeId>.md`）
- [ ] `weave_graph_status` 显示真实进度（节点/迭代/token）

## 2. 暂停/恢复（MVP-3 §六.2 回归）

- [ ] 运行中创建 `<productions>/PAUSE` → 当前角色跑完进入暂停
- [ ] 创建 `<productions>/RESUME` → 从下一角色续跑
- [ ] `pause-state.json` 内容正确（pauseReason/nextRoleId/completedSteps）

## 3. sendMessage / 消息流（MVP-3 §六.3 + MVP-4 零 token）

- [ ] A→父→B 跨会话消息可达
- [ ] `agent-message` 事件写入 RunLedger（只读桥接，不改变 sendMessage 行为）
- [ ] **验证零额外 token**：UI 展示消息流不注入任何 agent context

## 4. 观察者 L2（MVP-3 §六.4 + MVP-4 G7）

- [ ] 真实产出文件观察（git diff / 读文件）
- [ ] GREEN/YELLOW/RED 信号生成 + 写入 ledger `observer-signal`

## 5. 审批分级（MVP-3 §六.5 + MVP-4 G6）

- [ ] L1/L2/L3 分级请求；L1 超时自动继续
- [ ] approve/reject 能唤醒等待方

## 6. MVP-4 Web 看板（新增）

- [ ] D1 页头 "Weave 看板" 按钮可见（conversation.header slot）
- [ ] D3 主区切换为全屏看板（conversation.view slot）
- [ ] 图节点实时染色（SSE 推送 ≤2s）
- [ ] 点击节点看到最近 activity 行
- [ ] Token 分账真实数值非 0（按节点/角色）
- [ ] 审批面板待办 + 批准/拒绝 + 倒计时
- [ ] 观察者信号 GREEN/YELLOW/RED + 热力图
- [ ] 消息流 A→父→B 时间线可见（零 token）
- [ ] 暂停/恢复/终止从 Web 触发生效
- [ ] 运行历史 + checkpoint 恢复

## 7. 冒烟检查点

- [ ] 重启 DSH web 后看板仍可访问（插件热重载无泄漏）
- [ ] 无 `ctx.webServer` 环境（headless/TUI）下插件正常加载（ctx.get 轮询静默跳过）
