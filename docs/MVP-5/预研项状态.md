# MVP-5 预研项状态（PR-5.x）

> 版本：v1（2026-09-25 第 4 轮）
> 依据：`docs/MVP-5/MVP-5-task-union.md` 第十三节

| # | 预研项 | 状态 | 当前实现/待测说明 |
|---|---|---|---|
| PR-5.1 | ctx.tokenMeter API | ✅ 已具备（MVP-4） | REST /tokens + TokenCollector（byNode/byRole/total） |
| PR-5.2 | session 日志读取（zstd） | ⚠️ 已具备 traces jsonl | 引擎轨迹流式落盘 + log-reader；zstd session 日志未接（可选兜底） |
| PR-5.3 | DSH subagent 视图跳转 API | ⏳ 待真实环境探测 | 当前 best-effort `#/subagent/<id>` hash 跳转；需探测 `openSubagent`/路由 API |
| PR-5.4 | ctx.subagents.interrupt 签名 | ✅ 已具备（MVP-4 waiter） | waitForSubagentEnd 使用 ancestor interrupt；PauseError 处理 |
| PR-5.5 | DSH subagent 事件名 | ✅ 已具备（MVP-4） | subagent/end、node-activity 桥接 |
| PR-5.6 | DSH Slots API 激活 conversation.view | ⏳ 待真实环境探测 | 当前看板内以抽屉实现（WeaveEditPanel）；自动右侧滑出需探测 `slots.activate` 签名 |
| PR-5.7 | ask_user_question 子代理路由 | ⏳ 待真实环境探测 | 后端 answer REST 已备；弹窗 UI 需探测路由 |
| PR-5.8 | DSH UI primitives | 🟡 未开始 | 当前用原生 HTML/CSS 内联样式 |
| PR-5.9 | React Flow 版本兼容 + DnD | 🟡 未开始（可选） | 当前原生 div+SVG 实现可演进 React Flow |
| PR-5.10 | DSH 主题令牌 | 🟡 未开始 | 当前内联样式 |

## 真实环境探测清单（用户侧执行）

1. **PR-5.6**：在 3081 页面控制台执行 `window.dispatchEvent(new CustomEvent('weave:task-proposed',{detail:{taskId:'task-x'}}))`，确认 WeaveEditPanel 是否弹出；若否，探测 `ctx.slots.activate` 是否存在及签名。
2. **PR-5.3**：真实运行图后拿到 childId，尝试 `window.open('#/subagent/'+childId)` 是否可打开子代理页。
3. **PR-5.7**：让子代理调 `ask_user_question`，观察是否产生 `graph/paused(pauseReason=awaiting-user)`，再经 `POST /tasks/:id/answer` 恢复。
4. **Phase H E2E**：一句话 → weave_propose_task → 面板编辑 → 开始 → 看板实时染色 → 暂停/恢复/终止。

## 代码内已有测试覆盖

- routes.spec（5）：/tasks 列表、cancel、start 守卫、/roles、/graphs 保存+列表
- propose-commands.spec（3）、task-store（11）、canvas-model（4）、token-collector（5）、log-reader（3）、graph-store（3）、role-library（4）
