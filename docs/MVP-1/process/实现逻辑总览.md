# MVP-1 实现逻辑总览

> 版本：v1（2026-09-22）｜目标读者：开发者/维护者
> 目标：说清楚「角色 YAML → 可执行协作链」这一条 MVP-1 主线的实现逻辑与关键决策

## 一、主线架构

```
角色 YAML (roles/*.yaml) + skill 文件 (skills/*/SKILL.md)
        │ ① Zod 校验（role-schema.ts）
        ▼
RoleDefinition（纯数据）
        │ ② 编译（role-loader.ts：读 skill、路径逃逸校验、字段映射）
        ▼
RoleProfile → ③ 包装为 SubagentProvider（start() 注入 persona/toolFilter/agentOptions → 委托 spawn）
        │ ④ 注册到 ctx.subagents（src/index.ts apply）
        ▼
SubagentProvider[]（6 个角色 provider，插件卸载自动撤销）
        │ ⑤ 触发（weave_run_chain 工具 → chain-runner.ts）
        ▼
runChain() 串行调用 ctx.subagents.start(角色ID, request)
        │ ⑥ 每步：角色独立 Session（记忆隔离）→ 产物落盘 productions/<角色ID>/
        ▼
R1→R2→R4→R6→R7→R8 六阶段交付物
```

## 二、源码模块与职责

| 模块 | 文件 | 职责 | 关键接口 |
|---|---|---|---|
| 插件入口 | `src/index.ts` | 解析 rolesDir/skillsDir（基于插件包根，不依赖 cwd）；注册角色 provider + 工具 | `apply(ctx, config)` |
| 共享类型 | `src/shared/types.ts` | RoleDefinition / RoleProfile / ObserverConfig / 错误类 + Zod Schema | `RoleDefinitionSchema` |
| 结构化日志 | `src/shared/logger.ts` | JSON 结构化输出 + AsyncLocalStorage trace 贯穿 + 脱敏工具 | `logger.*` / `withTrace` |
| 角色 Schema | `src/l3-roles/role-schema.ts` | YAML 文本 → Zod 校验 → RoleDefinition | `parseRoleYaml` |
| 角色编译器 | `src/l3-roles/role-loader.ts` | skill 加载 + 路径逃逸防护 + 字段映射 + SubagentProvider 包装 | `compileRoleToProvider` / `compileRoleProfile` |
| 单链编排 | `src/l2-engine/chain-runner.ts` | 串行执行角色链 + 产物落盘 + 上游摘要传递 + **实时日志/心跳/STOP 中止** | `runChain` / `chainLog` |
| 链定义 | `src/l2-engine/mvp1-chain.ts` | R1→R8 六阶段 prompt 模板 | `buildMvp1Chain` |
| 验证工具 | `src/l2-engine/chain-tool.ts` | `weave_run_chain` 工具（模型可调用入口） | `registerChainTool` |
| Client 空壳 | `src/client/index.tsx` | MVP-1 占位（MVP-4 实现 UI） | `apply` |

## 三、关键契约与决策（D-001）

### 3.1 SubagentProvider 官方契约（与设计文档的偏差）

设计文档假设 `SubagentProvider` 的 `capabilities` 携带 `persona/toolFilter/agentOptions` 值；
**官方 0.1.5-rc.2 实际是**：

```typescript
interface SubagentProvider {
  name: string;
  capabilities: SubagentCapabilities;   // 布尔标志：agentOptions/outputSchema/depthLimit/toolFilter/persona
  inheritsParentContext: boolean;        // 描述性，非服务校验
  agentRouteDefaults?: { provider, model };
  start(request: ResolvedSubagentStartRequest): Promise<SubagentRun>;  // 必需
  prepareContinuable?(...): ...;         // 可选
}
```

- `persona` / `toolFilter` / `agentOptions` / `maxDepth` 属于 **`SubagentStartRequest`**（每次 start 传入）
- 因此角色以「**包装 provider**」存在：`start()` 在请求中注入角色字段后委托给底层 `spawn` provider

### 3.2 workflowEngine 无法按角色选择 provider

官方 `dsh-workflow-worker-thread` 的 `agent(prompt, opts)` 仅支持
`{label, phase, schema, provider, model}`，其中 `provider` 是 **LLM 路由覆盖**
（进入 `agentOptions.provider`），**不是** `ctx.subagents.start(name)` 的 provider 选择。

→ MVP-1 单链改为**自写编排**（`chain-runner`）+ `weave_run_chain` 工具触发，
不依赖 workflowEngine 的按角色切 provider。

### 3.3 模型路由 🐛

- `llm-pi-ai` 用**模型 ID**（`DeepSeek-V4-Flash`）做解析键，**非 name**（`deepseek-v4-flash`）
- 角色 YAML 的 `model.model` 必须写 ID：`DeepSeek-V4-Flash`（token 消耗小的 flash）

### 3.4 路径解析

- `rolesDir` / `skillsDir` 默认基于**插件包根**（`import.meta.url`），不依赖 `process.cwd()`
- `system_prompt_ref` 相对路径基于 skillsDir 解析；编译期校验路径不逃逸 skillsDir

## 四、记忆隔离与 toolFilter 的实现

| 机制 | 实现 | 验证 |
|---|---|---|
| 记忆隔离（G4） | 角色 provider `inheritsParentContext: false` + spawn 每次全新 session | 6 子代理独立 session 文件 + 各带独立首条 prompt |
| toolFilter | 角色 provider `start()` 注入 `toolFilter: { allow: [...role.tools] }` | 官方 `applyChildComposition` → `childCtx.tools.restrict()` |
| 上游交接 | 下游 prompt 只携带上游「摘要 + 文件路径」 | chain 日志 `upstream_count` 0→5 递增 |

### 4.1 工具最小权限（防跑偏 + 省 token）

| 角色 | 工具集 | 理由 |
|---|---|---|
| R1-requirement | `[]`（零工具） | 链首，无上游可读 |
| R2 / R4 / R6 / R7 / R8 | `['read']` | 仅读上游产物；**禁止** pwsh/glob/grep/write/edit |

> 🐛 教训：曾给 R6 完整工具集（含 `pwsh`），它反复勘察 node/npm/tsc 环境，
> 5+ 步无产出、烧 11 万 token。**工具权限 = 探索空间**，最小权限使输入 token ↓50%。

## 五、可观测性与用户控制（用户自助，不依赖 AI 监看）

| 能力 | 实现 | 用户用法 |
|---|---|---|
| 实时日志 | `chainLog()` 写 `<productionsRoot>/chain.log` + stdout | `Get-Content ...\chain.log -Encoding UTF8 -Wait -Tail 30` |
| 心跳 | 每 10s 写「仍在执行 + `elapsed_s`」 | 区分「在干活」与「卡死」 |
| 中止 | 阶段边界检查 `<productionsRoot>/STOP` 文件 | `New-Item ...\productions\STOP -Force` |
| 恢复 | 删除 STOP 文件 | `Remove-Item ...\productions\STOP` |
| 便捷脚本 | `test-env/{watch,stop,resume}-chain.ps1`（UTF-8 BOM） | 独立 PowerShell 窗口运行，零 agent token |

## 六、产物形态

| 角色 | 产物 | 说明 |
|---|---|---|
| R1 / R2 / R4 / R7 / R8 | `*.md` | Markdown 文档（不剥离代码块） |
| **R6** | **`index.html`**（或其他代码扩展名） | 纯代码产物 → **自动剥离 ` ``` ` 包裹**，保证可直接运行 |

## 七、与后续 MVP 的衔接

| MVP-1 产出 | MVP-2 如何使用 |
|---|---|
| `compileRoleToProvider`（SubagentProvider 包装） | StateGraph 节点通过 `roleRef` 引用已注册角色 |
| `RoleDefinitionSchema` | 图 DSL 的 `roleRef` 指向角色 id |
| `chain-runner` 产物落盘 | `art://` 工件引用基于 `productions/<角色ID>/` |
| `chain.log` 可观测模式 | 演进为 RunLedger 事件流（对齐 OTel） |
| 记忆隔离验证 | 节点执行继续复用 `inheritsParentContext=false` |
| `weave_run_chain` 工具 | 由 StateGraph 引擎取代（MVP-2 质变点） |
| 遗留坑（7 条） | 见 `MVP-1阶段总结与遗留.md` §三 |

---
**关联**：`docs/decisions/D-001-subagent-provider-contract.md`、`docs/MVP-1/0-设计.md`、`docs/MVP-1/1-DevTask.md`、
`docs/MVP-1/process/MVP-1阶段总结与遗留.md`、`docs/MVP-1/process/链执行监控与停止.md`
